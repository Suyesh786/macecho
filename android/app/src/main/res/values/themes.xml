<resources xmlns:tools="http://schemas.android.com/tools">
    <!--
        Material Components theme — required by the Material dependency added in Phase 4.
        NoActionBar variant is used because MainActivity manages its own toolbar-free layout.
    -->
    <style name="Theme.MacEcho" parent="Theme.MaterialComponents.Light.NoActionBar" />
</resources>
